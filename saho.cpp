#include"iostream"
using namespace std;
int main ()
{

        int ch, no1, no2, res;
        cout<<"select 1 for Add two number \n select 2 for sub of two number"<<;
        cin>>ch;
        cout<<" Enter Two number ";
        cin>>ch;
        cout<<"Enter two number "<<;
        cin>>no1>>no2;
        switch(ch)
        {
            case 1;
                res=no1+no2;
        }

}
